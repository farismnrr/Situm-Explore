# 2026-09-03

## Floorplan asset recovery

- User urgently asked to locate the Gedung floor 1/floor 2 layout assets that had disappeared.
- Read-only search found the original JPEGs in `/home/farismnrr/.local/share/Trash/files/`: `lt 1.jpeg`, `lt 1-1422.jpeg`, `lt 2.jpeg`, `lt 2-1422.jpeg`, `lt 2-matched.jpeg`, `lt2bigGedung-Model-narrow.jpg`, and `lt2bigGedung-Model-small.jpg`.
- `lt 1-1422.jpeg` and `lt 2-1422.jpeg` match the former Git resources `resources/buildings/building-1422/source/floor-1.jpeg` and `floor-2.jpeg` byte-for-byte by SHA-256. The originals are currently absent from `Downloads` and the repository current tree; no file was restored or moved.
- No matching candidates were found in the checked project directories, alternate home Trash, or named backup/cache locations. Next step is user-directed: copy/recover the selected files to a safe project or recovery location without emptying Trash or overwriting the dirty worktree.

## Public floorplan placement

- User asked to place the large floor 1/floor 2 layouts in the project's public assets and keep them out of Git.
- Copied the Trash files `lt 1.jpeg` and `lt 2.jpeg` byte-for-byte to `public/building-layouts/gedung-lt1-big.jpeg` and `public/building-layouts/gedung-lt2-big.jpeg`.
- Added the narrow ignore rule `public/building-layouts/`; `git check-ignore` confirms both files are ignored. `git diff --check` passed, and no commit or push was performed.

## Blender layout reference

- User asked to load the public floor 1/floor 2 layouts into Blender before changing the design.
- Created `/tmp/situm-explore-layout-reference.blend` with both JPEGs as packed, selectable image references, labeled floor frames, and a top-view comparison camera; launched it in Blender 5.2.1 GUI.
- No tracked source, plan, or existing dirty change was modified. Blender MCP socket was unavailable, so the scene was prepared through Blender's local Python interface.
- Next step is user review of the open scene and direction on which floor/layout treatment to change.

## 2D-to-3D navigation direction

- User wants to redesign the 2D floorplans and eventually produce a 3D indoor-navigation layout, with internet references where useful.
- Checked official Situm Cartography and React Native references, OGC IndoorGML navigation semantics, and Blender glTF/GLB export guidance. The working boundary is visual Blender geometry plus semantic space/transition metadata; Situm cartography and path graph remain navigation authority.
- Created `/tmp/situm-explore-layout-blockout-v0.blend` from the packed reference scene: normalized 2D space cells, inferred wall segments, room labels, navigation-candidate spines, and stair transition candidates for both floors. Scene metadata marks scale and inferred paths as provisional.
- No live Situm data, API credentials, runtime source, or active Plan 043 implementation was changed. Next step is user review of the 2D blockout before 3D extrusion and calibration to live building dimensions.

## Design-only checkpoint scope

- User narrowed the work to two checkpoints only: a proper 2D layout, then a 3D map; external product/runtime concerns are out of scope for this design pass.
- Created `/tmp/situm-explore-layout-2d-proper-v1.blend` with clean floor/room color blocks, architectural wall layers, door swings, stair treads, basic furniture/fixtures, circulation accents, labels, legend, and a toggleable original underlay.
- Verified the file as `Checkpoint 1 - proper 2D layout` with separate room, wall, detail, and presentation collections. 3D extrusion has not started; awaiting user review of the 2D design.

## Blender window constraint

- User requested that only one Blender instance remain open to avoid excessive laptop load.
- Verified only `/tmp/situm-explore-layout-2d-proper-v1.blend` is currently running; future Blender work will reuse that instance/scene.

## Photo-aligned 2D revision

- User supplied named room photos and asked to adjust the layout only, ignoring furniture and component contents.
- Created `/tmp/situm-explore-layout-2d-photo-aligned-v2.blend` from the clean V1 scene: Floor 1 now reflects enclosed/glass-partitioned workrooms and a glass entry; Floor 2 keeps the long open workroom and adds a central clear aisle; both floors use a U-stair/landing core; kitchen/restroom/corridor zones remain explicit.
- Packed the two public floorplan references plus nine supplied photos into the `.blend` as retained reference images and recorded a text index. The final scene metadata marks the design checkpoint as `1 - photo-aligned proper 2D layout`, with component contents intentionally omitted.
- Replaced the old GUI process with exactly one Blender instance showing V2. V1 remains at `/tmp/situm-explore-layout-2d-proper-v1.blend`; no repository source or runtime code was changed. 3D map work has not started.

## Photo correction and 3D approval gate

- User corrected the interpretation: `down-stairs.jpeg` shows a storage/service area beside the stairs, not an indoor stair-down route on LT1.
- Created `/tmp/situm-explore-layout-2d-photo-corrected-v3.blend`: removed every LT1 stair/tread/handrail object, replaced it with `F1 | SERVICE OUTDOOR AREA` marked `navigation_included=False` and `NON-ROUTE`; retained the actual LT2 stair core.
- Replaced the door leaves and swing arcs with 24 small portal/jamb markers across 8 wall openings. Wall gaps remain authoritative for future 3D openings.
- Explicitly recorded `three_d_approval_required=True` and `three_d_status=not started; requires explicit user approval`. Exactly one Blender GUI process now shows V3; no 3D geometry was implemented.

## Vertical stair direction correction

- User clarified that the stair footprint shown in the LT1 plan is the lower end of the connection to LT2; the mistake was treating it as a stair-down absence and deleting it.
- Created `/tmp/situm-explore-layout-2d-photo-corrected-v4.blend`: restored the LT1 stair footprint as an external/open stair labeled `STAIR UP TO LT2`, retained the matching LT2 footprint labeled `STAIR DOWN TO LT1`, and kept the storage/service interpretation of `down-stairs.jpeg` as adjacent context.
- V4 validation confirms 26 LT1 stair elements, 34 LT2 stair elements, no swing-arc/door-leaf objects, 24 clean portal markers, and 11 packed reference images. No 3D geometry or 3D implementation was started; explicit user approval remains required. Exactly one Blender GUI process now shows V4.

## In-repository Blender generator

- User asked to keep the Blender generator inside the Situm Explore repository for later editing.
- Added `scripts/blender/generate_source_traced_layout.py` as a standalone Blender 5.x generator. It starts from a factory scene, reads the public denah/photo assets, recreates the source-traced 2D layout, packs available references, and accepts an optional `--output` path after Blender's `--` separator.
- Generator validation succeeded with 11 packed references, zero kitchen door portals, zero swing-arc/door-leaf objects, and explicit LT1 UP/LT2 DOWN stair metadata. It does not contain a 3D generation step and records the explicit approval gate in the output scene.
- Opened the generated `/tmp/situm-explore-layout-source-traced-generated.blend` in exactly one Blender GUI process. Existing dirty `.gitignore`, public photos, and session files were preserved; no commit or push was performed.

## LT1 kitchen/walkway correction with both floors visible

- User clarified that F2 must remain visible; focusing on LT1 means changing LT1 first, not isolating or hiding the second floor.
- Updated `scripts/blender/generate_source_traced_layout.py` so `--floor 1` marks the edit focus only. Both floor drawings remain visible and the comparison camera stays centered on F1 and F2.
- Generated `/tmp/situm-explore-layout-lt1-focus-both-visible-v7.blend`. Validation confirms F1 and F2 are fully visible, the single `F1 | KITCHEN SET + WALKWAY` zone has 12 vertices, F1 has no kitchen partition or kitchen portal, and the LT1 stair remains present. 3D is still gated behind explicit user approval; no commit or push was performed.

## LT1 kitchen fill and label correction

- User clarified that the large notch between the left kitchen strip and the lower walkway is also kitchen, while the lower strip should be labeled only `WALKWAY`.
- Widened the unified F1 kitchen/walkway polygon to the left side of the stair, changed the visible labels to `KITCHEN` and `WALKWAY`, and kept F2 visible.
- Generated `/tmp/situm-explore-layout-lt1-focus-both-visible-v8.blend`. Validation confirms F1/F2 visibility, the expanded F1 kitchen zone, no F1 kitchen partition/portal, and the retained `STAIR UP TO LT2`. One Blender GUI process shows v8; no 3D or repository commit/push was performed.

## LT1 single-up stair correction

- User clarified that LT1 must show only the left-side stair going UP; the right side is an adjacent gray clear area, and the top gap should be filled as part of the stair/clear-area core.
- Added a dedicated single-flight LT1 stair generator: it removes the second flight and down marker, extends the stair to the top boundary, and keeps the right-hand clear area gray through the same upper boundary. F2's separate stair representation was left unchanged.
- Generated `/tmp/situm-explore-layout-lt1-single-up-both-visible-v9.blend`. Validation confirms no LT1 down markers, only UP stair roles, both floors visible, 11 packed references, and 3D still not started. One Blender GUI process shows v9; no repository commit/push was performed.

## LT1 equal stair and clear-area sizing

- User clarified that the LT1 stair and the gray area beside it must have equal width; the kitchen should absorb the released width.
- Shifted the equal-width pair to the right while keeping the workroom boundary fixed: LT1 kitchen now reaches normalized x=4.45, the single UP stair spans x=4.45..5.65, and the gray clear area spans x=5.65..6.85. Both are 1.20 normalized units wide and extend to the top boundary.
- Generated `/tmp/situm-explore-layout-lt1-equal-stair-clear-v10.blend`. Validation confirms no LT1 down markers, both floors visible, 11 packed references, and 3D still not started. One Blender GUI process shows v10; no repository commit/push was performed.

## LT2 kitchen, walkway, and bathroom repositioning

- User moved the review to LT2 and clarified that its kitchen should follow LT1's open kitchen-to-walkway treatment, with the bathroom positioned in front of/above the kitchen.
- Replaced the isolated F2 kitchen strip and bottom arrival apron with one open `F2 | KITCHEN + WALKWAY` polygon. Removed the full-height kitchen partition, retained only the bathroom enclosure boundary, and kept the bathroom portal opening toward the kitchen.
- Generated `/tmp/situm-explore-layout-lt2-kitchen-walkway-v11.blend`. Validation confirms the unified F2 zone, no F2 kitchen partition/portal, the retained bathroom portal, unchanged LT1 equal stair/clear sizing, both floors visible, and 3D still not started. One Blender GUI process shows v11; no repository commit/push was performed.

## LT2 open workroom label centering

- User asked to center the `OPEN WORKROOM` annotation in the LT2 workroom area.
- Moved the label from the right side to the exact center of the open-workroom footprint while leaving the traced room geometry unchanged.
- Generated `/tmp/situm-explore-layout-lt2-open-workroom-centered-v12.blend`. Validation confirms label center `(15.25, 2.72)` against room center `(15.25, 2.73)`, both floors visible, and 3D still not started. One Blender GUI process shows v12; no repository commit/push was performed.

## LT2 open workroom cleanup

- User identified the selected rectangle inside the LT2 open workroom as an unwanted central-clear-aisle overlay.
- Removed `F2 | CENTRAL CLEAR AISLE` and its `CLEAR AISLE` label from the generator; the open workroom footprint and centered `OPEN WORKROOM` label remain.
- Generated `/tmp/situm-explore-layout-lt2-open-workroom-clean-v13.blend`. Validation confirms the overlay and label are absent, the open workroom remains, both floors are visible, and 3D is still not started. One Blender GUI process shows v13; no repository commit/push was performed.

## Separate 3D layout implementation

- User approved the current 2D checkpoint and requested a commit followed by 3D implementation in a new file.
- Committed the approved 2D generator/assets as `9f56f65` (`feat(blender): add editable 2d layout generator`).
- Added `scripts/blender/generate_3d_layout.py`, which opens the approved v13 scene, preserves its 2D collections hidden, stacks F1/F2 vertically, creates colored floor panels, extruded solid/glass walls, clean 3D door portals, and a single navigable LT1 UP stair connector, then saves separately.
- Generated and validated `/tmp/situm-explore-layout-3d-v1.blend`: 8 zone panels, 23 wall segments, 5 door portal frames, 18 stair steps, 11 labels, explicit semantic metadata, and no render/runtime integration. The v13 2D source remains unchanged. One Blender GUI process shows the new 3D file; the 3D generator is ready for a separate commit.

## 3D stair detail pass

- User requested the stair to be built as part of the 3D layout rather than remaining a coarse blockout.
- Updated `scripts/blender/generate_3d_layout.py` to add 18 individual riser/step volumes, 18 raised tread caps, two handrails, six railing posts, lower/upper landings, and an explicit F1-to-F2 UP transition object.
- Generated `/tmp/situm-explore-layout-3d-stair-v2.blend`. Validation confirms stair steps span z=0.16..3.20, tread caps and handrails are present, the source 2D file remains preserved, and the 3D output is separate. One Blender GUI process shows v2; no runtime integration or render was performed.

## 3D side-by-side review and source-matched stair correction

- User requested that the 3D floors be shown left-to-right instead of stacked vertically, and flagged that the stair geometry did not match the supplied plans.
- Updated `scripts/blender/generate_3d_layout.py` to keep Floor 1 at its source X range `-21.5..-1.5` and Floor 2 at `2.5..22.5`, both at display Z=0 in a clearly marked side-by-side review mode. The separate 3D file still retains the approved 2D collections hidden and untouched.
- Kept LT1 as the approved single left-side UP flight with the adjacent clear area separate. Added LT2's source-matched two-flight stair with a central landing, two semantic transition markers, solid steps/tread caps, landings, railings, and posts.
- Generated `/tmp/situm-explore-layout-3d-side-by-side-stair-v3.blend`. Validation confirms 18 LT1 steps, 9+9 LT2 flight steps, 36 tread caps, five landings, seven handrails, 18 posts, two review-only transitions, side-by-side slab bounds, and preserved 2D source metadata. One Blender GUI process shows v3; no render or runtime integration was performed.

## LT2 stair shift and workroom reduction

- User confirmed that the LT1 and LT2 stair entries are not aligned and requested that LT2 move right, with the kitchen enlarged and the open workroom reduced.
- Updated `scripts/blender/generate_source_traced_layout.py` with editable LT2 shift constants. The kitchen boundary and stair left edge move from normalized x=3.30 to x=4.45; the stair right edge and open-workroom boundary move from x=5.75 to x=6.90. The workroom therefore keeps the outer boundary but becomes 1.15 normalized units narrower. The LT2 workroom glass entry and portal move with that boundary.
- Generated `/tmp/situm-explore-layout-lt2-stair-shifted-v14.blend`; validation confirms both floors remain visible, the shifted stair bounds are local x=4.45..6.90, the open workroom starts at x=6.90, and there are still zero kitchen door portals.
- Regenerated `/tmp/situm-explore-layout-3d-side-by-side-stair-v4.blend` from v14. Validation confirms the side-by-side slabs are unchanged, the LT2 workroom panel starts at world x=9.40, the two-flight stair remains present, and the 2D source is preserved. One Blender GUI process shows v4; no render or runtime integration was performed.

## LT2 restroom stretch to stair

- User clarified that the LT2 workroom position is already correct and only the restroom/toilet should be enlarged into the empty strip before the stair.
- Updated `scripts/blender/generate_source_traced_layout.py` so the restroom right edge moves from normalized x=3.15 to x=4.45, exactly meeting the LT2 stair left edge. The restroom door and component contents remain unchanged; the restroom label is recentered.
- Generated `/tmp/situm-explore-layout-lt2-restroom-stretched-v15.blend`; validation confirms restroom bounds local x=1.45..4.45, stair bounds local x=4.45..6.90, workroom bounds unchanged at x=6.90..19.75, both floors visible, and zero kitchen door portals.
- Regenerated `/tmp/situm-explore-layout-3d-side-by-side-stair-v5.blend` from v15. Validation confirms the 3D restroom panel meets the stair edge, the workroom remains unchanged, both semantic stair transitions remain, and the 2D source is preserved. One Blender GUI process shows v5; no render or runtime integration was performed.

## LT1 centered double entry

- User requested that the LT1 entrance become a centered two-door entrance and that the top of the door frame align with the surrounding wall top.
- Updated `scripts/blender/generate_source_traced_layout.py` so the F1 entry opening is centered in the entry bay at normalized y=3.025, with a 1.20-wide double-door marker and center mullion. Both the entry glass partition gap and the outer-wall gap use the same centered opening.
- Updated `scripts/blender/generate_3d_layout.py` to read the double-door metadata and build two vertical door leaves plus a center mullion. The entrance header top is set to the wall top (`Z=2.87`) rather than leaving the previous gap above the door frame; other portals retain their normal height.
- Generated `/tmp/situm-explore-layout-lt1-centered-double-entry-v16.blend` and `/tmp/situm-explore-layout-3d-side-by-side-stair-v6.blend`. Validation confirms one double door, two door leaves, one mullion, centered opening, header/wall top alignment, unchanged LT2 workroom, preserved source 2D, and one Blender GUI process showing v6. No render or runtime integration was performed.

## LT1 entrance center and transparent door correction

- User flagged that the entrance was still visually left of center and that the double doors appeared opaque.
- Updated `scripts/blender/generate_source_traced_layout.py` to center the LT1 opening on the full exterior wall span (`y=0..5`, center `y=2.50`) instead of the narrower entry bay. The opening remains 1.20 wide and both wall gaps stay aligned.
- Updated `scripts/blender/generate_3d_layout.py` to use a tinted cyan transparent door material (`alpha=0.34`, `Transmission Weight=0.25`, Blender surface method `DITHERED`) for the two door leaves. The center mullion remains solid and the header still reaches the wall top.
- Generated `/tmp/situm-explore-layout-lt1-centered-double-entry-wall-span-v17.blend` and `/tmp/situm-explore-layout-3d-side-by-side-stair-v7.blend`. Validation confirms the opening is centered at y=2.50, the header and entry wall both top at Z=2.87, there are two tinted transparent door leaves and one mullion, and the LT2 workroom remains unchanged. One Blender GUI process shows v7 in Material Preview; no render or runtime integration was performed.

## LT1 Workroom 1 door removal

- User flagged an unwanted door/frame in front of Workroom 1 and requested its removal.
- Updated `scripts/blender/generate_source_traced_layout.py` so the F1 Workroom 1 wall opening remains as an open passage but no longer creates a door portal or frame. Workroom 2 and the centered double main entry remain unchanged.
- Generated `/tmp/situm-explore-layout-lt1-remove-workroom1-door-v18.blend`. Validation confirms no `F1 | workroom 1 door` objects, two remaining Workroom 1 wall segments around the opening, and zero kitchen door portals.
- Regenerated `/tmp/situm-explore-layout-3d-side-by-side-stair-v8.blend`. Validation confirms four portal frames, no Workroom 1 door objects, two main-entry leaves with one mullion, unchanged side-by-side display and stair/restroom metadata, and preserved 2D source collections. One Blender GUI process shows v8 in Material Preview; no render or runtime integration was performed.

## LT1 workroom order and near-entrance door correction

- User clarified that the room nearest the main entrance is Workroom 1, and that the near-entrance room still showed an unwanted second door.
- Corrected the source-traced LT1 room order: the near-entrance footprint is now `WORKROOM 1`, the farther footprint is `WORKROOM 2`. Removed the near-entrance Workroom 1 portal while retaining the farther Workroom 2 portal and the centered double main entrance.
- Generated `/tmp/situm-explore-layout-lt1-workroom-order-and-near-entry-door-removal-v19.blend`. Validation confirms the revised semantic room order, no F1 Workroom 1 door objects, the retained F1 Workroom 2 portal, zero kitchen door portals, and unchanged centered main-entry metadata.
- Regenerated `/tmp/situm-explore-layout-3d-side-by-side-stair-v9.blend`. Validation confirms no 3D Workroom 1 door objects, four portal frames total, two main-entry leaves with one mullion, preserved open wall passages, unchanged side-by-side display and stair/restroom metadata, and preserved 2D source collections. One Blender GUI process shows v9 in Material Preview; no render or runtime integration was performed.

## Blender GUI handoff correction

- User reported that the visible Blender scene had not changed. Investigation found the earlier launch had loaded v9 in a non-visible/offscreen process because the desktop display environment was missing.
- Terminated that process and relaunched `/tmp/situm-explore-layout-3d-side-by-side-stair-v9.blend` with the active Wayland desktop environment, keeping only one Blender process. The scene contents were already correct; this corrected the GUI handoff rather than changing the layout again.

## Canonical Blender file and visible Workroom 1 label correction

- User reported that the viewport still showed Workroom 1 on the far side and Workroom 2 near the entrance. Inspection found the zone geometry had been renamed but the presentation-label coordinates/body strings remained hardcoded in the old order.
- Swapped the visible F1 label bodies and positions so the far room is `WORKROOM 2` and the room nearest the entrance is `WORKROOM 1`. Kept the near-entrance Workroom 1 portal removed and the farther Workroom 2 portal retained.
- Changed the 3D generator defaults to one stable canonical output, `/tmp/situm-explore-layout.blend`, instead of revisioned review filenames. Regenerated the source-traced scene into the generator's intermediate source path, then saved the final 3D scene at the canonical path.
- Final canonical-file validation confirms visible labels at the expected world positions (`WORKROOM 2` at x=-11.82 and `WORKROOM 1` at x=-6.17), zero Workroom 1 door objects, the retained Workroom 2 portal, two main-entry leaves, 36 stair steps, side-by-side display, and one Blender GUI opening the canonical file.

## Workroom 1 second opening removal

- User reported that Workroom 1 still appeared to have two door-sized openings. Inspection of the canonical source found the near-entrance room had both a front/corridor wall opening and an entry-side partition opening.
- Removed the Workroom 1 front/corridor wall opening as well as its already-removed portal frame. The entry-side partition opening remains as the single intended access; Workroom 2's portal and the main double entrance remain unchanged.
- Regenerated `/tmp/situm-explore-layout.blend` in place. Validation confirms one continuous Workroom 1 corridor wall segment, two entry-partition segments around the remaining access, zero Workroom 1 door objects, four total portal frames, 22 wall segments, 36 stair steps, and the corrected visible room labels. One Blender GUI process now opens the refreshed canonical file.

## Workroom 1 opening direction correction

- User clarified that the previous opening was closed in the wrong position: the entry-side opening needed to be removed, while the front/corridor opening should remain.
- Restored the Workroom 1 corridor opening and closed the entry glass partition as requested. The room remains nearest the entrance, has no door portal/frame, and now has only the intended single corridor access.
- Regenerated `/tmp/situm-explore-layout.blend` in place and reloaded it into the single Blender GUI. Validation confirms two Workroom 1 corridor wall segments around the remaining opening, one continuous entry partition, zero Workroom 1 door objects, corrected labels, and unchanged Workroom 2/main-entry/stair structure.

## Workroom 1 matching glass door

- User requested that Workroom 1's door match Workroom 2: positioned close to the room edge, same size/top alignment, and fitted with glass.
- Added matching normalized door geometry for both rooms: clear width `0.72` and right-edge margin `0.48`. Workroom 1 now has a near-right-edge corridor portal at normalized x=`16.95..17.67`, with the same `2.15` jamb height used by Workroom 2.
- Added a transparent tinted-cyan single glass leaf for the Workroom 1 3D portal. The canonical 3D file now reports five portal frames, three door leaves including the new Workroom 1 glass leaf, and unchanged stair/room geometry.
- Regenerated `/tmp/situm-explore-layout.blend` in place and reloaded the single Blender GUI. Validation confirms both workroom zone dimensions remain `5.65 x 3.45`, equal frame sizes/heights, the Workroom 1 glass material, corrected label positions, and preserved side-by-side display.

## Workroom door flanking and upper glass transom

- User requested a `door | divider | door` arrangement instead of both doors being right-aligned, plus a small glass panel above the Workroom 1 door.
- Kept Workroom 2's door near its right edge and moved Workroom 1's matching door near its left edge, so the two portals flank the shared center divider. Both clear widths are `0.72`, both right/left insets are `0.48`, and both jamb tops align at the same height.
- Added a transparent tinted-cyan Workroom 1 glass leaf and an upper transom glass panel that closes the space from the door header to the wall top.
- Regenerated `/tmp/situm-explore-layout.blend` in place and reloaded the single Blender GUI. Validation confirms equal `5.65 x 3.45` room footprints, matched portal frames, Workroom 1 glass leaf/transom, corrected flanking positions, and unchanged stair/room structure.

## Componentized doors, stairs, and stair landing

- User supplied `public/building-photos/stair-landing.jpeg` as the reference for the LT1/LT2 stair junction and asked for reusable LEGO-like components so one instance can be edited without changing every other instance.
- Refactored the 3D generator so each of the five door placements and two stair placements gets its own component collection, unique mesh data, and moveable `COMPONENT ROOT` empty. Door and stair builders remain reusable while placements remain independent.
- Added two independent stair-landing assemblies: F1 bottom stair access and F2 upper stair access. Each has a lower tinted-glass door panel, white frame, and upper transom glass matching the supplied reference style.
- Added `COMPONENTS | README` inside the Blender file with the component editing workflow. The approved 2D footprint and side-by-side review mode remain unchanged.
- Regenerated `/tmp/situm-explore-layout.blend`. Validation confirms 9 component collections/roots, 6 unique door mesh data blocks, 2 stair-landing assemblies, 2 stair-landing transoms, zero kitchen door portals, 36 stair steps, and one Blender GUI process showing the canonical file. No render or runtime integration was performed.

## Stair landing reference correction

- User clarified that `stair-landing.jpeg` shows an open stair flight and landing, not a door to be placed on the stairs.
- Removed the stair-access glass/door assembly from the generator and canonical scene. The stair remains an independent component with open bottom/top/central landings, treads, risers, and handrails.
- Repacked the current stair reference and regenerated `/tmp/situm-explore-layout.blend`. Validation confirms 7 component collections/roots (5 doors plus 2 stairs), 5 open stair landings, 0 stair landing doors, 0 stair landing transoms, unchanged 36-step geometry, and one Blender GUI process showing the refreshed canonical file.

## Continuous return-stair correction

- User flagged that the refreshed 3D stair still did not match the supplied landing photos. The LT2 generator had two flights split into separate middle sections, so they read as disconnected strips rather than a real return stair.
- Updated `scripts/blender/generate_3d_layout.py` so LT2 uses two parallel, connected flights: the lower right flight rises to an open far landing, then the upper left flight returns in the opposite direction. LT1 remains the single left-side UP flight; its open top/bottom landings now span the stair plus the adjacent clear-area width.
- Regenerated `/tmp/situm-explore-layout.blend`. Validation confirms the two LT2 flights share the far landing (`Y=4.26..4.78`), span the same travel corridor in opposite directions, retain 36 total steps and 5 open landings, and contain 0 stair doors/transoms/frame objects. The file was reloaded in the single Blender GUI process.

## Stair direction and review-axis correction

- User clarified the intended match when the floors are combined: LT1 uses the left stair with its U-turn at the top; LT2 uses the left flight going UP and the right flight going DOWN.
- Updated both generators to preserve that orientation. The LT2 split now follows the LT1 stair width, with the left flight carrying `UP` metadata and the right return flight carrying `DOWN` metadata. LT1 has an explicit open top U-turn landing/handrail.
- Changed the 3D review arrangement from left/right to top/bottom: Floor 1 is displayed above Floor 2 using only a Y-axis review offset, both remain at the same display Z, and their X footprints align for screenshot comparison. Regenerated `/tmp/situm-explore-layout.blend` and reloaded the single Blender GUI.
- Validation confirms 18 LT1 UP steps, 9 LT2 left UP steps, 9 LT2 right DOWN steps, aligned X origins, 0 stair door/frame/transom objects, 36 total steps, 5 open landings, 8 handrails, and 22 posts.

## Interior door style from Door Style.jpeg

- User supplied `public/building-photos/Door Style.jpeg` and requested that its visual treatment be applied to every door except the entrance.
- Packed the reference into the source-traced Blender scene and recorded the intended form: lower tinted glass door panel, white frame/header, and upper glass transom.
- Generalized the 3D generator across the four non-entrance portals: F1 Workroom 1, F1 Workroom 2, F2 restroom, and F2 open workroom. The centered F1 double entrance remains on its original entrance material and has no transom.
- Regenerated `/tmp/situm-explore-layout.blend` in place. Validation confirms 5 portal frames, 6 door leaves total, 4 interior glass leaves, 4 interior transoms, 22 wall segments, 36 stair steps, unchanged workroom dimensions/labels, and side-by-side floor display. One Blender GUI process shows the canonical file; no render or runtime integration was performed.

## Shared LT1/LT2 return-stair design

- User clarified that the LT1 stair must use the same design as LT2. The former LT1 gray side-clear strip is now the right-hand return flight, so both source floorplans show the same left-UP/right-DOWN return-stair form with an open top U-turn landing.
- Updated `scripts/blender/generate_source_traced_layout.py` so both floor stair drawings use the same `stair()` implementation and the same normalized left-flight width. LT1 now occupies the combined former stair and side-clear footprint; its source label also reads `LEFT UP / RIGHT DOWN`.
- Replaced the duplicated LT1/LT2 3D stair construction in `scripts/blender/generate_3d_layout.py` with the shared `build_return_stair_component()` builder. Each floor still has an independent component collection/root, but both instances now carry the same `RETURN_STAIR` design family, flight directions, landing structure, and rail structure.
- Regenerated `/tmp/situm-explore-layout.blend`. Validation confirms 36 stair steps (18 per floor), 36 tread caps, 6 open landings (3 per floor), 10 handrails (5 per floor), 32 posts (16 per floor), zero stair door/frame/transom objects, and top-bottom Y-axis review mode with Floor 1 above Floor 2. No render or runtime integration was performed.

## LT2 office zone and kitchen low partition

- User requested the LT2 door be pushed to the wall, the walkway be removed so the area after the door is entirely office, `OPEN WORKROOM` be renamed to `WORKROOM 3`, and a low divider be added beside the north kitchen equipment area.
- Updated `scripts/blender/generate_source_traced_layout.py`: LT2 now has a `WORKROOM 3` zone extending to the lower floor boundary (`y=0.25..4.75`), no LT2 walkway label/zone, and the Workroom 3 door sits flush at the lower end of the left wall (`y=0.25..1.37`). The restroom door remains source-positioned for now.
- Added an LT2 stair-facing kitchen equipment divider with a `0.25` wall-height ratio. Updated `scripts/blender/generate_3d_layout.py` to carry wall height ratios into 3D, producing a low partition of `0.6875` against the normal `2.75` wall height.
- Regenerated `/tmp/situm-explore-layout.blend`. Validation confirms no LT2 `WALKWAY` objects, `WORKROOM 3` labels/zones, one `low_partition` object at the requested height, unchanged shared stair counts, preserved F1, and no render or runtime integration.

## LT2 Workroom 3 open door

- User requested that the LT2 Workroom 3 door be shown open for the visual review instead of closed.
- Added a reusable `door_state`/`door_open_side` path to the source portal and 3D door builder. The Workroom 3 lower tinted-glass leaf now hinges at jamb A and swings inward into the room; its frame, header, and upper glass transom remain in place. The entrance and all other doors remain closed.
- Regenerated `/tmp/situm-explore-layout.blend`. Validation confirms one open door, five total portal frames, four interior glass leaves/transoms, unchanged 36-step shared stairs, and the existing LT2 low partition/workroom zone. One Blender GUI process was reloaded with the canonical file; no render or runtime integration was performed.

## All doors open with moving leaf frames

- User clarified that every door should be open and that the door frame must move with the glass leaf, not remain as glass-only geometry.
- Extended both source portal helpers with explicit door state/open-side metadata and marked all five portal placements open: the F1 entrance, both F1 workroom doors, the F2 restroom door, and the F2 Workroom 3 door. The F2 restroom swing side is explicitly directed into the restroom.
- Extended the reusable 3D door builder with a moving perimeter frame for every leaf (two side rails plus top and bottom rails). Fixed portal jambs, headers, thresholds, and upper transoms remain as the wall-side opening frame; the lower door leaf and its perimeter frame share the open swing placement.
- Regenerated `/tmp/situm-explore-layout.blend`. Validation confirms five open door components, six open leaves, 24 moving leaf-frame parts, five portal frames, four interior transoms, and unchanged 36-step stairs. One Blender GUI process will be reloaded with the canonical file; no render or runtime integration was performed.
