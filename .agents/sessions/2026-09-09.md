# Session — 2026-09-09

## Plan 044 — Web / Native Map UI Parity

- User asked to redesign the Situm Explore web Map to match the already-custom native Map and then explicitly authorized implementation.
- Audited native `NativeMapScreen.tsx` / `CustomIndoorMap.tsx` against web `map.vue` / `SitumViewer.vue`; confirmed native owns floorplan/POI/gesture/navigation presentation while web was still an embedded Viewer.
- Created `plan/044-web-native-map-parity` from `origin/main`; preserved unrelated pre-existing Drizzle/schema working-tree edits without modifying or staging them.
- Replaced `/app/map` presentation with an app-owned responsive renderer over the existing authenticated workspace cartography response. Added native-style search/floor dock, compact collision-aware POI labels, destination sheet, pan/pinch/wheel zoom, map-tap dismissal, reset/fullscreen, and native app handoff.
- Kept browser capability truthful: no synthetic blue dot, sensor positioning, ETA, rerouting, or turn-by-turn guidance. The browser Map does not receive the workspace Only Read key; cartography remains server-mediated.
- Updated product/architecture/data-source docs and durable decisions to supersede the old capable-layout Viewer + phone Map gate policy.
- Focused validation executed: modified-map ESLint PASS, `npm run typecheck` PASS, `npm run build` PASS. Authenticated browser visual/runtime acceptance was not executed in this tool context and is not claimed.

## Plan 044 staging deploy recovery

- User asked to recreate the local production-style container after Plan 044. Clean image build passed, but GHCR push failed with 403, so deployment used a local immutable-style tag.
- First recreate exposed a real runtime mismatch: login returned 500 because committed Drizzle queried `situm_explore.users`, while the actual `situm_explore` database contains the application tables in PostgreSQL `public`.
- Confirmed the user's pre-existing unstaged Drizzle/schema edits intentionally switch the ORM/migrations to `public`; those edits were not committed or staged by the agent.
- Built and deployed local compatibility image `sha-a3f4bf1a1665-publicschema-local` from Plan 044 plus those user-owned working-tree edits. Container on port 3005 is healthy; liveness passes; dummy and `owner@example.com` wrong-password login smokes return 401 with no query error.
- GHCR `staging` remains unchanged because local registry credentials currently receive 403 on push. Authenticated Map visual acceptance remains the next runtime check.
