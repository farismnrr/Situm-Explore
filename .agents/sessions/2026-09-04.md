# 2026-09-04 — Blender floorplan export

- User requested repaired light-mode LT1/LT2 PNG exports from `.tools/output/source-traced.blend`, with complete framing, readable labels, preserved canonical layout, and visual verification.
- Inspected the canonical scene: LT1 content bounds `-21.590,-0.090..-1.410,5.531` with 62 visible mesh/curve/font objects (7 FONT); LT2 bounds `2.410,-0.090..22.590,5.531` with 53 objects (5 FONT). Confirmed the old camera used Blender orthographic scale incorrectly and the source had a dark world with no lights.
- Updated `scripts/blender/export_floorplan_png.py` to measure evaluated world-space bounds, derive proportional safety margins, use Blender's horizontal `ortho_scale` correctly, force view-layer updates, isolate each floor, set 1:1 pixel aspect, and use temporary white-world/emission material copies for light mode without saving the source scene.
- Rendered and visually inspected both outputs, including edge/detail crops. Projection validation found all target bounds inside normalized `0.0413..0.9587` / `0.0971..0.9029` and no foreign-floor intersections. Source reopen showed original dark world/material/camera and no `EXPORT |` objects/materials.
- Outputs remain local ignored artifacts under `.tools/output/floorplan-png/`; no commit, push, merge, or unrelated cleanup was performed. No unresolved visual issue observed.

## Follow-up — F2 Workroom 3 door / stair clearance

- User reported that the F2 Workroom 3 door portal visually occupied the stair area. Source inspection confirmed the portal opening was `y=0.25..1.37` while the F2 stair began at `y=1.02`; the evaluated door/stair bounds overlapped.
- Updated `scripts/blender/generate_source_traced_layout.py` only for this portal relationship: the F2 stair bounds now have named constants, the Workroom 3 clear opening is constrained to `0.72` normalized units or the stair edge minus `0.06` clearance, whichever is smaller, and the existing open-inward door state is preserved. Room footprint, stair footprint, orientation, labels, and other doors were not moved.
- Regenerated the canonical `.tools/output/source-traced.blend`, then rerendered both light-mode PNGs with the existing bounds-driven exporter. New evaluated bounds are door `9.400,0.216..9.550,0.994` and stair `6.908,1.020..9.442,4.680`; door/stair bbox overlap is false.
- Visually inspected both full-resolution exports and a focused LT2 door/stair crop. Outer boundaries, labels, door markers, stair details, light background, contrast, and floor isolation remained acceptable; the LT2 portal now stops below the stair with a visible gap.
- Door-clearance iteration checks: canonical source SHA after export was `610971ef1fda26725520b9434307d3212d5d9f3c56fe7363d3e9c53d752dedff`; `ruff check` and `git diff --check` passed; canonical source contained no `EXPORT |` objects/materials. The later Situm-bound crop dimensions are recorded below.

## Follow-up — Situm-bound PNG crop

- User requested Situm-ready images whose canvas is the floor layout itself, without the outer title or presentation margin. The exporter now frames structural non-FONT content, hides per-floor plan titles outside that boundary, renders with a small temporary safety border, and trims the final RGB PNG with ImageMagick (`magick`/`convert`). Labels inside the layout remain part of the map.
- Rerendered both outputs successfully at `3190x820` with the outer wall at the canvas edge. A second `-trim` check returned the same `3190x820+0+0` for both files, confirming no remaining presentation margin. Full images and LT2 door/stair detail were visually inspected after the final rerender.
- Final PNG sizes are LT1 `1,455,641` bytes and LT2 `1,493,675` bytes. Existing canonical source SHA remained `610971ef1fda26725520b9434307d3212d5d9f3c56fe7363d3e9c53d752dedff`; no temporary export objects/materials were saved into the source.

## Follow-up — 3D/source alignment audit

- User reported that the PNG did not line up with the Blender 3D review. Audit found the review `.blend` and GLBs were stale: `layout-3d.blend` and both GLBs were from before the F2 Workroom 3 door clearance change. The old 3D door header still occupied normalized `y=0.25..1.37`, while the current source uses `y=0.25..0.96` and leaves the stair clearance.
- Updated `scripts/blender/generate_3d_layout.py` so the 3D door-position metadata is inherited from the current source property instead of retaining the old hardcoded range. Regenerated `layout-3d.blend` from the current canonical source and regenerated both floor-scoped GLBs from that review file.
- Post-regeneration audit: 3D F2 door header is `(-14.638,0.250..-14.562,0.960)`, jamb bounds stop at `0.998` including bevel, metadata reports `y=0.25..0.96`, and the review file points to the current source. Source structural bounds and generated review bounds remain aligned after the expected top-bottom display translation.
- Visually inspected exact-resolution top-down renders of the updated 3D review and re-imported GLBs against both final PNGs, including a focused LT2 stair/door crop. The footprints, stair placement, room divisions, orientation, and door location align; GLBs import without a mirror/rotation and retain the normalized horizontal `20.2 x 5.2` footprint. Temporary comparison renders were removed afterward.
- Final artifact validation: PNGs remain RGB `3190x820`, ImageMagick trim remains `3190x820+0+0` for both, GLBs are valid glTF binary v2 files, `ruff check` and `git diff --check` pass. No commit, push, merge, or unrelated cleanup was performed.

## Follow-up — Situm editor aspect audit

- User reported that the newly uploaded F2 floorplan still looked horizontally compressed in the Situm Map Editor. Rechecked the local artifacts: both PNGs are RGB `3190x820` with an exact pixel ratio of `3.89024`, and the exporter still uses an orthographic top-down camera with `pixel_aspect_x/y = 1.0`; no non-uniform raster resize was introduced.
- Visually re-inspected both local PNGs, the user-provided Situm screenshot, and the currently open authenticated Situm Map Editor tab in read-only mode. The live map renders the floorplan through a MapLibre canvas; the selected F2 content and labels match the uploaded LT2 artifact. The editor About panel reported map `bearing 0°` and `pitch 0°`, so the observed aspect discrepancy is not explained by a tilted browser viewport.
- Checked the official Situm floorplan guidance: floorplan updates retain existing calibration, all floorplans share the same 2D building area, and building layout dimensions/corners/rotation define the map canvas. The evidence points to the existing Situm building/corner calibration or physical canvas ratio, not the local PNG. No Situm save, upload, delete, or other external mutation was performed.
- Resolution for this follow-up: keep the verified PNGs unchanged; correct the Situm building/floorplan placement with a uniform scale and matching real-world canvas dimensions/corners. Do not squash the PNG or delete the floor again, because that would hide the mapping issue and could remove calibration data.
- Clarified the exporter module docstring so it accurately documents the implemented behavior: structural bounds drive framing, internal labels stay visible, and presentation-only external FONT titles are excluded from the Situm canvas.

## Follow-up — Publish LT1 floorplan for Situm testing

- User requested a MinIO URL for LT1 only. Uploaded the already-verified local PNG to the existing `shared-minio` deployment using its in-container `mc` client; no new bucket, service, credential, or application configuration was created.
- Published object: `situm-explore/cartography/floorplans/2026-09-04-v1/floorplan-lt1.png`, with `Content-Type: image/png` and immutable public cache metadata.
- Public GET verification from the host returned HTTP `200`, `image/png`, `1,455,641` bytes, and `3190x820` RGB PNG data. The downloaded public object SHA-256 exactly matched the local artifact `bbd822da6e178a334d8000260d58c542036f91c692f1b3c42c2f3feaf35ef05f`.
- The first GET attempt through the MCP relay could not connect to the public hostname; host-network verification succeeded afterward. This was an observer-network issue, not an upload failure. LT2 was not uploaded.

## Follow-up — Publish LT1 3D GLB for Situm testing

- User clarified that the requested MinIO URL was for the 3D asset, not the 2D PNG. Uploaded only the current LT1 GLB from `.tools/output/glb/situm-explore-lt1.glb` to the existing `shared-minio` deployment.
- Published object: `situm-explore/cartography/3d/2026-09-04-3d-v2/situm-explore-lt1.glb`, with `Content-Type: model/gltf-binary` and immutable public cache metadata.
- Local source and public GET both identify a glTF binary v2 of `180,316` bytes with matching SHA-256 `e73efcc100b52884e5f3351d599e513cd74731f364867f363eea62b1e5089e9e`. Public HTTP verification returned `200` and `model/gltf-binary`.
- LT2 was not uploaded and no Situm cartography mutation was performed.

## Follow-up — Situm 3D model scaling diagnosis

- User clarified that the question is whether Situm itself converts or non-uniformly shrinks the GLB during upload/render, rather than whether the local file is valid.
- Checked Situm's official 3D Models documentation: Situm accepts a GLB URL and exposes model-level `Scale` (size in relation to the map), `Height`, and `Orientation`; the documentation does not describe an automatic GLB rewrite or an X-only conversion on upload.
- Checked Situm's official Cartography SDK documentation: raster floorplans expose a separate `Scale (pixels / meter)` value, and floorplan dimensions in meters are derived from image pixels and that floor scale. This is independent from the 3D model's `Scale` control.
- The previous local checks proved upload/file integrity and GLB geometry consistency (valid glTF binary v2, re-imported horizontal footprint about `20.2 x 5.2`, no exporter mirror/rotation/non-uniform X transform); they did not prove Situm's model-to-map placement scale.
- The user screenshot shows Situm `Scale=1` and an oblique 3D preview with an orientation angle of `-31`; screen-space proportions in that view are not a reliable top-down metric comparison. Current evidence points to Situm model placement/calibration or 3D projection, not a changed GLB file. No Situm save or other external mutation was performed.

## Follow-up — Situm 2D canvas dimensions

- User supplied the Situm Building & Floor Info screen showing automatically populated canvas dimensions `Width=17.1 m` and `Height=6.2 m`.
- Official Situm documentation defines these as building/floorplan canvas dimensions, with the canvas corners, center, and rotation determining the map area; updating a floorplan preserves existing calibration. It does not establish that the values are recalculated from the newly uploaded PNG.
- The final local floorplan PNGs are `3190x820`, ratio `3.89024`, and are trimmed to the outer layout with no presentation margin. Situm's displayed dimension ratio is `2.75806`; if `17.1x6.2` is used as the full image canvas, preserving the PNG height would imply width about `24.1195 m`, so `17.1 m` would represent about `29.10%` horizontal compression. This is a canvas aspect mismatch, not evidence that the PNG bytes were altered.
- The canonical 3D scene is intentionally normalized to a 20x5 sheet and explicitly records physical meter calibration as pending. Therefore 3D is authoritative for layout/proportion, but not by itself for absolute meter values. No Situm dimensions or calibration were changed.

## Follow-up — Publish LT2 3D GLB for Situm testing

- User explicitly authorized publishing the LT2 3D asset to MinIO; no Situm settings, floorplan PNGs, or Blender geometry were changed.
- Published object: `situm-explore/cartography/3d/2026-09-04-3d-v2/situm-explore-lt2.glb`, using the existing `shared-minio` deployment and the existing public `situm-explore` bucket policy.
- The object metadata is `Content-Type: model/gltf-binary` with immutable public cache control. Public GET returned HTTP `200`, `155,996` bytes, and glTF binary v2 identification.
- Local and public SHA-256 both equal `2487eeca0024ccc6a0165171dbd8cda5b06e6e498bfee4fcc3dfaf6e7eaa5a6b`. No Situm upload/save or cartography mutation was performed.


## Closure integration — parallel Plan 042 / Plan 043

- Plan 042 and Plan 043 were developed as sibling branches from the same Plan 041 baseline. Plan 043 therefore accumulated its accepted mobile map changes before the Plan 042 maintainability ratchet existed on that branch.
- During local closure, Plan 042 was integrated first. Applying its frozen pre-043 baseline to the already-complete parallel Plan 043 state correctly reported the map files as new/grown legacy debt. Rather than rewrite accepted map behavior solely to retroactively satisfy a governance rule that did not exist during that implementation, the mobile baseline was rebased once to the integrated Plan 043 state. Future work may not grow those ceilings and must lower them when debt improves.
- Building reference photos remain local-only and ignored; the Plan 043 integration is squashed without `public/building-photos/*` blobs so those photos do not enter `main` history.
