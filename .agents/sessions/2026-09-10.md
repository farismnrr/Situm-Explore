# 2026-09-10 — Plan 044 3D Explore

- User rejected the interim top-down floorplan experience and explicitly required `/app/map` to be 3D-only, eye-level / Street-View-like exploration with no 2D fallback. Missing 3D capability should surface an error instead of silently degrading.
- Reused the repository Blender pipeline to regenerate the tracked building's floor-scoped GLB assets. Staging mounts generated GLBs read-only from `.data/3d-models` into `/var/lib/situm-explore/3d`.
- Added an authenticated workspace-scoped 3D model endpoint. The existing `/api/workspaces/**` catch-all router initially swallowed the nested route, so the 3D dispatch was also wired into that router through a shared server utility.
- Added a Three.js/WebGL `IndoorWalkCanvas` with a 1.62 m perspective camera, mouse-look, WASD/arrow/touch movement, reset, and deterministic destination travel. Explore has no top-down floorplan, image, iframe, or Viewer fallback.
- Current Situm cartography for the active building returned zero POIs. Destination search therefore uses canonical semantic room objects embedded in the trusted GLB (`Main Entry`, kitchens, restrooms, workrooms) rather than inventing Situm POIs.
- Fixed a client lifecycle race where fresh authenticated navigation could leave Explore at the shell without triggering the workspace-scoped cartography fetch.
- Focused ESLint and Nuxt typecheck pass; production Docker build passes.
- Authenticated Playwright smoke used the user-provided owner account. F2 loaded a valid `200 model/gltf-binary` GLB and WebGL canvas; selecting `Workroom 3` and pressing `Go` changed camera X from 8.450 to 3.325 while Y remained 1.620. F1 then loaded its own valid GLB; selecting `Workroom 1` changed X from 8.401 to 5.325 while Y remained 1.620.
- Temporary Playwright scripts/screenshots were removed after testing. Process inspection confirmed zero Chrome/Chromium/Playwright browser processes remained.
- Final task commit `523affb4c049d17630fe94d73956572b142be686` was pushed to `origin/plan/044-web-native-map-parity` as `feat(web): ship 3d-only Explore walkthrough`.
- The user's pre-existing Drizzle/schema changes for PostgreSQL `public` remain unstaged/uncommitted and must not be included in the task commit. They are still required in the local staging build until their ownership/integration is resolved.
- Rebuilt through the canonical `make image-build` flow with immutable local tag `sha-523affb4c049-e2e-local`, intentionally including the user's existing public-schema compatibility worktree because staging requires it; those DB files remain unstaged/uncommitted. Force-recreated the local staging service and verified container health plus `/api/health/liveness` = 200. GHCR push remains unavailable from this host due 403 authorization.
- User owns the next visual E2E pass; no further browser automation was run after the requested rebuild.

## Plan 044 closure

- User explicitly requested closure with full governance before proceeding to the next stage.
- Full branch review removed the superseded 2D renderer and duplicate 3D API route, extracted semantic-room discovery from the 3D component to keep it below the source-size ceiling, and relocated the 3D model server helper so existing directory debt did not grow.
- Dependency closure refreshed Nuxt UI/transitive resolutions; clean `npm ci` and final Engineering Guard audit report 0 vulnerabilities.
- User-owned Drizzle/schema compatibility edits were stashed during clean-branch closure validation. The first `web full` guard stopped at policy because `.tmp-tests` still existed; the task-owned temporary artifacts were inspected and deleted. The final `Engineering Guard web full` then passed policy, lint, typecheck, production build, and audit.
- `python3 .agents/scripts/maintainability.py web` passed on its single closure run: 134 files scanned, 0 file debt, 0 function debt, 2 unchanged baseline directory-debt entries, 0 bypass debt.
- Plan 044 is closure-complete on its branch; PR/merge remain user-gated. User visual E2E is not claimed as executed. Closure evidence is recorded in `.agents/evidence/plan-044-closure-2026-09-10.md`.
- Closure commit `b39c25c6e7ae6242ab4b18b4f4e7653fae898052` was pushed to origin. The user-owned PostgreSQL `public`-schema Drizzle/schema stash was then restored without conflicts and remains unstaged/uncommitted.
- Rebuilt the final local runtime as `sha-b39c25c6e7ae-publicschema-local`, intentionally combining closure source with the required restored DB compatibility worktree. Force-recreated only the `situm-explore` web service with `--no-deps`; container health is `healthy`, `/api/health/liveness` = 200, and `/` = 200. No Playwright/browser automation was opened during this final closure rebuild. GHCR push authorization remains blocked by 403.

## Plan 044 integration

- User explicitly authorized PR creation and integration after closure.
- PR #42 (`feat(web): ship 3D-only Explore walkthrough`) was created from `plan/044-web-native-map-parity` into `main`; GitHub reported the head mergeable/clean and GitGuardian Security Checks passed.
- PR #42 was merged with merge strategy at merge commit `420f399bf10979f389f36f686d056ce3e4b77012`, with expected head `02d5816d6ced4996850249338ec613be9fa845cf`.
- Local `main` was fast-forwarded to the merge. The user's unrelated PostgreSQL `public`-schema Drizzle/schema work was stashed only for the branch switch and must be restored after this governance-only integration record is pushed.

## Plan 045 planning — LT1 orientation diagnosis + 2D-primary Explore

- User changed the next product direction: browser Explore should default to the app-owned 2D floorplan and expose the WebGL walkthrough through an explicit `Digital Twin 3D` action. 3D failure after explicit opt-in must remain an explicit error rather than a silent fallback.
- User reported a release-blocking LT1 visual defect described as the floor being top/bottom reversed while LT2 looks correct. Leader performed read-only diagnosis and did not modify application/runtime/Blender source.
- Authenticated staging cartography confirmed building `19922` with F1 floor `70343` level `0` and F2 floor `70345` level `1`. The live Situm floorplan downloads are byte-identical to the generated canonical local PNGs: F1 SHA-256 `bbd822da6e178a334d8000260d58c542036f91c692f1b3c42c2f3feaf35ef05f`, F2 SHA-256 `cbb93b379a6067c28d94d462b6b9b0a276eb516b270918e469a5515ea913a893`.
- Verified the application model mapping is not swapped: floor level `0 -> lt1`, level `1 -> lt2`; server slot mapping resolves those to their matching filenames. The healthy staging container mounts `.data/3d-models` read-only and its observed GLB hashes match the host runtime files.
- GLB audit found zero negative-determinant/reflection node transforms for both floors. Runtime `.data/3d-models` files differ from the earlier `.tools/output/glb` files only in JSON metadata; their binary geometry chunks are byte-identical to the previously verified exports. No evidence supports an agent-generated LT1 mesh mirror.
- Blender pipeline audit confirmed `generate_3d_layout.py::transform_point()` only translates review coordinates and `export_situm_glb.py` only applies floor-center normalization before standard glTF Y-up export. The effective source-to-runtime horizontal mapping has the same sign on both floors: runtime `X = source X - center X`, runtime `Z = center Y - source Y`. No LT1-only axis inversion exists.
- CSS/Three.js audit found no F1-specific negative scale, `scaleY(-1)`, model rotation, or other mirror. `GLTFLoader` adds `gltf.scene` directly.
- The initial `setSpawn()` path is asymmetrical: F1 has a semantic Entrance and derives camera spawn from the `GLASS ENTRY` destination bounds, while F2 lacks Entrance and uses the model-edge fallback. This is a real runtime presentation-contract defect. However, reconstructed initial poses (`F1 ~= x8.401,z0.493`, `F2 ~= x8.450,z0.000`, both eye height 1.620 m looking to model center) prove it is not a hidden 180-degree yaw difference.
- Runtime-equivalent headless camera probes showed the F1 semantic-entry spawn can create materially misleading/occluded framing compared with F2. The implementation direction is therefore to replace semantic-room-derived orientation with one deterministic canonical floor-view/spawn contract. If the exact user-perceived top/bottom reversal remains after that known asymmetry is removed, execution must stop and collect new evidence rather than mirror/rotate LT1 by guesswork.
- Created `plans/045-web-explore-2d-digital-twin-orientation.md` as the next execution authority and marked it active/planning-complete in `.agents/state.md` and `plans/README.md`. Plan 044 remains immutable historical evidence of the previously accepted 3D-only milestone.
- Plan 045 explicitly prohibits arbitrary LT1 negative-scale/mirror or magic 180-degree rotation fixes without new reproducible evidence. It requires app-owned 2D as the default, lazy 3D opt-in, deterministic 3D spawn/reset orientation, F1 canonical-anchor visual acceptance, F2 non-regression, explicit 3D failure semantics, and preservation of unrelated user-owned PostgreSQL/Drizzle work.

## Plan 045 implementation leader-review gate

- Implemented deterministic normalized-bounds 3D view contract shared by LT1/LT2; removed semantic Entrance from spawn/orientation authority. Final pose is normalized spawn x=0.50/z=0.78 looking to x=0.94/z=0.78 at eye height 1.620. Runtime-equivalent Chromium probes against the actual GLBs showed readable corridor framing on both floors and confirmed F1 canonical anchors in top-down evidence without any mirror/180-degree transform.
- Forward-ported the accepted pre-Plan-044 app-owned 2D floorplan renderer and real Situm POI/search/destination interactions; `/app/map` is 2D by default and explicit `?view=3d` lazy-mounts Digital Twin 3D. Added explicit Digital Twin 3D / 2D Map mode action and separate POI vs model-room selection state.
- Hardened 3D teardown with animation/listener cleanup, geometry/material disposal, renderer disposal + forced context loss, DOM removal, and reference clearing. Current architecture/design docs now describe the Plan 045 contract; Plan 044 remains historical.
- `npm ci` completed with 0 vulnerabilities. Focused ESLint, `npm run typecheck`, and `npm run build` passed. Authenticated production-browser acceptance was subsequently completed using a disposable migrated PostgreSQL container and the production Plan 045 image without mutating staging during that acceptance run. It proved default 2D makes zero GLB requests, explicit F1/F2 Digital Twin requests the correct model, repeated 2D/3D switching releases WebGL canvases, and forced GLB failure remains in explicit 3D error state. Full details are recorded in `.agents/evidence/plan-045-orientation-implementation-2026-09-10.md`.
- On 2026-09-11 the user manually inspected the checkpoint and reported the visual result as acceptable. The leader reviewed the implementation diff, runtime evidence, and orientation invariants; the leader did not independently repeat the user's manual browser visual inspection.
- Leader-review remediation then extracted Three.js diagnostics/resource disposal and Explore URL/style helpers without changing the accepted view contract. `IndoorWalkCanvas.vue` is now 349 lines and `app/pages/app/map.vue` 346 lines, both below the 350-line source ceiling. `git diff --check`, focused ESLint, typecheck, and production build passed; maintainability passed with `scanned=140 file_debt=0 function_debt=0 directory_debt=2 bypass_debt=0`.
- The pre-staging-schema-migration custom backup was copied from the disposable Plan 045 worktree to the primary durable project data area at `.data/backups/staging-pre-situm-schema-migration-20260911-065102.dump`, mode `0600`, SHA-256 `a08a3a6c01a75d6ae755729e620e4d87cdfc5187774846533ff53804a6adc168`; PostgreSQL 17.10 `pg_restore --list` successfully read the durable copy. No additional database mutation was performed during leader remediation.
- On explicit user-requested closure, `Engineering Guard web full` passed policy, full ESLint, Nuxt typecheck, production build, and dependency audit with 0 vulnerabilities. The single closure maintainability run passed with `scanned=140 file_debt=0 function_debt=0 directory_debt=2 bypass_debt=0`; the two directory entries are unchanged baseline debt. `.agents/scripts/validate.sh` and `git diff --check` passed, `.tmp-tests` was absent, and no Chrome/Chromium/Playwright process remained. Plan 045 is closure-complete on its branch; PR/merge remain a separate user gate.
