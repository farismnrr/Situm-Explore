# 2026-09-10 — Plan 044 3D Explore

- User rejected the interim top-down floorplan experience and explicitly required `/app/map` to be 3D-only, eye-level / Street-View-like exploration with no 2D fallback. Missing 3D capability should surface an error instead of silently degrading.
- Reused the repository Blender pipeline to regenerate the tracked building's floor-scoped GLB assets. Staging mounts generated GLBs read-only from `.data/3d-models` into `/var/lib/situm-explore/3d`.
- Added an authenticated workspace-scoped 3D model endpoint. The existing `/api/workspaces/**` catch-all router initially swallowed the nested route, so the 3D dispatch was also wired into that router through a shared server utility.
- Added a Three.js/WebGL `IndoorWalkCanvas` with a 1.62 m perspective camera, mouse-look, WASD/arrow/touch movement, reset, and deterministic destination travel. Explore has no top-down floorplan, image, iframe, or Viewer fallback.
- Current Situm cartography for the active building returned zero POIs. Destination search therefore uses canonical semantic room objects embedded in the trusted GLB (`Main Entry`, kitchens, restrooms, workrooms) rather than inventing Situm POIs.
- Fixed a client lifecycle race where fresh authenticated navigation could leave Explore at the shell without triggering the workspace-scoped cartography fetch.
- Focused ESLint and Nuxt typecheck pass; production Docker build passes.
- Authenticated Playwright smoke used the user-provided owner account. F2 loaded a valid `200 model/gltf-binary` GLB and WebGL canvas; selecting `Workroom 3` and pressing `Go` changed camera X from 8.450 to 3.325 while Y remained 1.620. F1 then loaded its own valid GLB; selecting `Workroom 1` changed X from 8.401 to 5.325 while Y remained 1.620.
- Temporary Playwright scripts/screenshots were removed after testing. Process inspection confirmed zero Chrome/Chromium/Playwright browser processes remained.
- Staging candidate before clean-commit rebuild was healthy on local image `sha-f48b5fa-3dwalk3-local`. GHCR push remains unavailable from this host due 403 authorization, so local immutable-image deployment is used for user verification.
- The user's pre-existing Drizzle/schema changes for PostgreSQL `public` remain unstaged/uncommitted and must not be included in the task commit. They are still required in the local staging build until their ownership/integration is resolved.
- Next action: commit/push only Plan 044 3D task files, rebuild/recreate staging from that exact commit plus the required local public-schema compatibility layer without modifying the user's DB changes, then hand visual E2E to the user.
