# Session — 2026-09-11

## Plan 046 planning and resource gathering

The user chose to keep the current app-owned browser Explore renderers: 2D `IndoorMapCanvas` remains primary and Three.js Digital Twin 3D remains explicit opt-in. Situm Map Viewer must not replace either product surface.

The next bounded work item is Plan 046 — Web Explore 2D Routing Foundation. Its purpose is to restore useful static POI-to-POI routing in the custom 2D map using real Situm POIs and real Situm wayfinding path nodes/links, validate A→B and B→A against Situm's own route as an oracle, and leave a renderer-independent route result for later 3D projection. Plan 046 does not implement 3D route geometry, AR, sensor-backed positioning, ETA, rerouting, or turn-by-turn guidance.

Resource gathering established:

- current `/app/map` does not mount `SitumViewer`; 2D and 3D are app-owned;
- current 3D `Go` behavior is camera travel to GLB semantic-room centers, not route guidance;
- current installed `@situm/sdk-js` is `0.25.0`; planning-time registry inspection observed `0.26.0` as latest;
- installed/current documented Viewer `startDirections` returns `Promise<void>` and does not provide raw computed route geometry to the custom renderer;
- existing authenticated workspace endpoints already expose real cartography and wayfinding `paths`;
- installed path types expose `PathLink.origin` as `both | source | target`, with direction semantics to be revalidated before implementation;
- historical Plan 041 proves app-owned graph routing over Situm paths is feasible, but its nearest-node snapping, symmetric traversal, and custom floor-transition penalty must not be copied blindly;
- Plan 046 should snap POI endpoints to eligible same-floor graph edges and compare the resulting topology to Situm's own route before 3D reuse;
- the reported 3D badge collision is deterministic because the walk badge and 2D switch occupy the same top-right coordinates; Plan 046 owns removing the redundant walk badge/live dot.

Planning artifacts created:

- `.agents/evidence/plan-046-resource-gathering-2026-09-11.md`
- `.agents/execution/plan-046-web-explore-2d-routing-foundation.md`
- `.agents/execution/prompt-execute-plan-046-web-explore-2d-routing-foundation.md`

`.agents/state.md` was reconciled to current Git truth: planning-time inspection observed Plan 045 / PR #43 integrated on `main` at merge commit `7782e7d`, and Plan 046 is now prepared for delegated execution but implementation has not started.

No product implementation was performed. Unrelated user-owned Drizzle/PostgreSQL/schema worktree modifications remain outside Plan 046 and must be preserved.

## Plan 046 execution — Phase 0 blocked before product implementation

Execution started on `plan/046-web-explore-2d-routing-foundation` from exact `origin/main`/Plan 045 merge `7782e7d4429868a7d3becd05fc0f9114f98225f1`. The unrelated user-owned Drizzle/PostgreSQL/schema modifications were preserved in place and not touched, staged, reset, cleaned, or stashed.

Phase 0 revalidated installed `@situm/sdk-js` `0.25.0`: `cartography.getPaths(params?: PathSearch)` supports `buildingId`, `PathNode` is `{ id, floorId, x, y }`, `PathLink` is `{ source, target, origin: 'both' | 'source' | 'target', tags, accessible }`, and Viewer `startDirections(...)` remains `Promise<void>` with no installed computed-route geometry event. Current official Situm wayfinding documentation confirms links are bidirectional by default, may be configured one-way, and tags can include/exclude segments from routing.

The mandatory Phase 0 route oracle could not be established in the current execution context. Historical Plan 019A/020 evidence records successful forward/reverse Situm Viewer route rendering, including `Pintu Masuk` and `Ruang Kerja Lt 2`, but does not preserve route geometry/topology; its temporary browser screenshots were intentionally removed under repository policy. Current staging is healthy but requires authentication, and this execution context has no reusable authenticated browser session or repository-stored plaintext login credential. Therefore a fresh Situm A→B and B→A reference route could not be observed or captured for topology comparison.

Per Plan 046's explicit Phase 0 exit criteria and blocker policy, no product implementation was started during that first execution attempt. Phases 1–6 remained unexecuted at that stop.

## Plan 046 execution resumed — Phase 0 oracle PASS

The user accepted the earlier stop as a legitimate evidence blocker and authorized a temporary local-only Situm Viewer validation harness. A short-lived authenticated application session was minted in memory only and used to call the existing owner-scoped Viewer auth path; no login password, Situm credential, cookie, or secret was persisted. The first configured building (`19922`) returned POIs but zero paths and was rejected for this proof. The second workspace supplied building `19870` (`Rukan Artha Gading Niaga`) with floor `69907` (`Base`), 5 real POIs, 1 path object, 22 nodes, and 29 links. All 29 links are `origin: both`, accessible, and untagged.

The fresh topology oracle pair is A = `1274449` / PT Perkasa Pilar Utama at `(65.88475066860696, 69.42530693939027)` and B = `1274451` / BCA Cab Artha Gading Niaga at `(100.12890526847639, 170.90430743293402)`, both on floor `69907`. The temporary Viewer rendered and screenshots captured both A→B and B→A. Evidence lives in `.agents/evidence/plan-046-phase0-oracle-2026-09-11.md` plus the two oracle PNGs. The harness was inline execution only and did not alter `/app/map` or leave a tracked validation file. Phase 0 is now PASS and product implementation may proceed in order.

## Plan 046 Phase 1 — 3D chrome cleanup

Removed only the redundant `3D Walk · <floor>` status badge and its decorative live dot from `IndoorWalkCanvas`, including the now-dead narrow-screen override. The actionable `2D Map` switch remains page-owned and untouched; loading/error cards, walkthrough hint, touch controls, mouse/keyboard movement, and Three.js lifecycle behavior were not redesigned. Focused ESLint and Nuxt typecheck passed after the change.

## Plan 046 Phase 2 — normalized route core

Added renderer-independent `app/utils/indoor-route.ts` and narrowed the shared `PathLink.origin` contract to the installed Situm union. Endpoints project onto the nearest eligible same-floor Situm edge, virtual snap positions split links without mutating source DTOs, and deterministic Dijkstra uses physical Cartesian segment length. Directionality follows the path origin end (`both` in both directions, `source` source→target, `target` target→source). The route preserves floor IDs, real graph node IDs, endpoint/snap/graph point kinds, and removes consecutive duplicate geometry. Invalid, missing-path, missing-snap, and disconnected inputs return explicit failures; no straight-line fallback exists. Cross-floor endpoints fail explicitly because Plan 046 has no evidence-backed transition weighting. A temporary `/tmp` white-box bundle against the real 19870 graph returned A→B node sequence `13 → 5 → 2 → 8`, with B→A exactly reversed; same-endpoint and empty-path inputs failed truthfully. The temporary bundle was removed.

## Plan 046 Phase 3 — path loading and shared route state

`useExploreIndoorRoute` owns start/destination IDs, normalized active route, request/error state, and real-current-building POI resolution above both renderers; workspace/building changes clear it, while floor/view changes do not. Endpoint replacement invalidates stale geometry before another calculation. The map-first POI card can assign the selected real Situm POI as Start or Destination, and a compact route card exposes only truthful endpoint/floor names plus Show route/Clear—no ETA, distance, steps, rerouting, or browser-location behavior. The 2D composition was extracted into `MapExplore2D` to keep the page orchestration bounded; route state remains page-owned. Focused ESLint and Nuxt typecheck passed.\n\nFollow-up live-contract correction: although the installed SDK type accepts `getPaths({ buildingId })`, the selected real venue's scoped URL returned Situm `404 entity_not_found` while the existing unscoped `getPaths()` returned the verified 1-path/22-node/29-link graph. Plan 046 therefore keeps both the server route and browser request on the previously verified authenticated unscoped `getPaths()` behavior, with the route core restricting traversal to the selected endpoints' same-floor links.

## Plan 046 Phase 4 — custom 2D route layer

`IndoorMapCanvas` now renders the shared normalized route as a pointer-inert SVG polyline inside the exact same transformed floor surface as the raster image. Route points reuse the existing physical mapping (`x / width`, `(length - y) / length`), so fit, pan, zoom, and reset act on raster and route as one surface rather than through separate camera math. Only consecutive points on the active floor are rendered. Start and Destination receive distinct marker/label roles plus role-specific accessibility labels; POI controls remain above the route layer. Focused ESLint and Nuxt typecheck passed.

## Plan 046 Phase 5/6 — production parity proof and Leader Review Gate

Authenticated production-preview acceptance used the fresh Phase 0 pair on building `19870` / floor `69907`: A `1274449` (PT Perkasa Pilar Utama) and B `1274451` (BCA Cab Artha Gading Niaga). Custom A→B and B→A screenshots were captured and compared to the fresh Situm Viewer references. Both custom routes materially follow the same configured corridor topology; the reverse route is the exact reverse graph chain because every selected link is `origin: both`. No manual route offsets or direct-line fallback were used.

Runtime checks also verified route/raster alignment under pan, zoom, and reset; endpoint replacement and clear; same-endpoint and empty-path failures without synthetic geometry; route-state survival through three `2D → 3D → 2D` cycles; zero GLB requests before explicit 3D opt-in; one WebGL canvas per explicit 3D mount; no route geometry rendered in 3D; absence of the redundant `3D Walk · <floor>` badge/live dot; and continued availability of the `2D Map` control. The oracle venue has one floor, so a physical same-route floor-switch-and-return check is not available there; source ownership remains floor-independent and cross-floor routing itself remains explicitly unsupported in Plan 046.

Final focused validation passed: `git diff --check`, `npm run lint`, `npm run typecheck`, and `npm run build`. The temporary `.tmp-tests/` validation workspace was removed before staging. Runtime evidence is recorded in `.agents/evidence/plan-046-runtime-acceptance-2026-09-11.md` with custom and Situm oracle screenshots. Plan 046 then stopped at the Leader Review Gate for parent review.

## Plan 046 leader review remediation

Leader review accepted the core route topology/oracle work but found one P1 stale-request race and two P2 hardening/documentation gaps. The route composable previously allowed an A→B request that completed late to restore obsolete route state after Clear, endpoint replacement, or a newer calculation. It now uses a monotonically increasing request generation and validates workspace, building, start POI, and destination POI before any async response/error can mutate active state. Clear, endpoint changes, invalid requests, workspace/building changes, and newer calculations invalidate older generations.

The generic route core also now fails closed on any eligible same-floor edge with non-empty Situm path tags because generic route-tag/profile semantics are not yet proven. No tag meaning is guessed. While making that boundary explicit, graph keys were hardened to include path-object identity so independent path objects returned by the unscoped Situm read cannot collide through repeated node IDs.

A temporary `.tmp-tests/` white-box harness proved Clear-during-request, endpoint-replacement-during-request, newer-request-supersedes-older-request, tagged-graph rejection, and repeated-node IDs across disconnected path objects. It printed `Plan 046 remediation white-box checks passed` and was removed immediately afterward. Current architecture, implementation, data-source, durable-decision, and routing-knowledge docs were reconciled to the implemented static web routing contract.

Post-remediation validation passed in the working tree: `git diff --check`, `npm run lint`, `npm run typecheck`, and `npm run build`. A second clean-source validation used a temporary `git archive HEAD` snapshot with only remediation files overlaid, explicitly excluding the user's unrelated Drizzle/PostgreSQL/schema modifications. After `npx nuxt prepare` generated the archive-local Nuxt config, clean-snapshot diff-check, lint, typecheck, and build all passed. Existing non-fatal Nuxt/Vite chunk-size/plugin-timing warnings remain unchanged.

Leader remediation evidence is `.agents/evidence/plan-046-leader-remediation-2026-09-11.md`. The Leader Review Gate now passes. Plan 046 is ready for explicit closure governance, but no PR, merge, branch deletion, closure run, or Plan 047 work is authorized by this remediation alone.

## Situm Wayfinding Path investigation

The user requested execution of an end-to-end Situm Path persistence investigation
for building `19954` / floor `70557`. The official dashboard login and Map Editor
reproduction succeeded, but Save returned `404 entity_not_found` for
`Path 19954`. Authenticated API probes, raw OpenAPI, installed SDK `0.25.0`, SDK
history through `v0.26.0`, and the current maps frontend all converge on the
building-scoped `PUT /api/v1/buildings/19954/paths` update route; no supported
create/initialization operation was found. No unrelated resource was mutated and
no credential/token/API key was persisted.

Durable artifacts written: `docs/research/situm-path-investigation.md`,
`docs/research/situm-paths.example.json`, and guarded `scripts/situm/upload-paths.sh`. The report classifies
the current state as BLOCKED BY SITUM BACKEND and includes sanitized evidence plus
support-ticket text. The remaining next step is Situm provisioning of the
building's missing Path aggregate or a supported create/upsert endpoint, followed
by rerunning the guarded uploader and UI verification.

## Situm Wayfinding Path investigation — Phase 2 legacy archaeology

The user requested a second pass over legacy/deprecated/internal-compatible
Situm Path mechanisms, with the existing report read first. Historical 2020
OpenAPI, current/deprecated specs, all published `@situm/sdk-js` packages
available through `0.26.0`, official mobile SDK sources, an archived 2024 maps
bundle, an archived dashboard shell, historical Situm hosts, auth compatibility
headers, and target-scoped compatibility requests were reviewed. The 2020
contract already had only Path GET plus building-scoped PUT; no legacy
create/initialize operation was found. A target-scoped PUT to the historical
default host returned the same `404 entity_not_found`, so no bootstrap
succeeded. Exact old editor/dashboard dynamic chunks were unavailable from the
archive and are recorded as limitations, not negative evidence.

Durable changes: appended `# Phase 2 — Legacy API Archaeology` and final
conclusion to `docs/research/situm-path-investigation.md`; added sanitized
`docs/research/situm-path-phase-2-findings.md`. No credentials, tokens, keys, cookies,
or unrelated resource mutations were persisted. Unresolved next step remains
Situm provisioning/repair of the target Path aggregate or provision of an
official create/upsert endpoint, followed by the guarded uploader and UI
verification.

## Situm Wayfinding Path investigation — Phase 3 legacy authentication

The user requested the third phase after the existing report was read: inspect
Dashboard API-key roles, create disposable keys, compare current/legacy auth,
revisit `POST /api/v1/paths`, and clean up every temporary credential. The UI
exposed `Disabled`, `Positioning`, `Only read`, `Cartography edition`, and
`Read & Write`. Two temporary keys were created, used only with target-bound
Path requests for building `19954`/floor `70557`, and deleted through the UI.

The Cartography key authenticated all safe reads and both documented JWT
exchange flows but returned `401` for the unscoped POST and `404 entity_not_found`
for scoped PUT. The Read & Write key authenticated the same reads and returned
`405 [GET]` for unscoped POST and `404 entity_not_found` for scoped PUT. A valid
owner Bearer also returned `405 [GET]`; anonymous/malformed Bearer returned
`401`. This answers the earlier 401 as generic authentication/permission
middleware behavior before method dispatch, not a usable legacy create route.

Cleanup was verified by reloading the Dashboard and by a final read-only API-key
metadata GET: exactly the five pre-existing keys remained, including the
pre-existing Map Editor key, with zero temporary-name matches. No Path data,
other building, unrelated resource, existing key, password, token, cookie, or
API-key value was persisted.

Durable changes: appended `# Phase 3 — Legacy Authentication & Temporary API
Keys` to `docs/research/situm-path-investigation.md`. The final conclusion remains
**BLOCKED BY SITUM BACKEND**; Situm provisioning or an official create/upsert
endpoint is still required.

## Plan 046 explicit closure

The user requested Plan 046 closure after the staging login failure was traced to an accidental Docker build-context inclusion of unrelated uncommitted public-schema files. Staging was rebuilt from clean committed Plan 046 source and recreated healthy; the database-backed invalid-login smoke returned 401 instead of the prior 500. Before final governance, `app/pages/app/map.vue` was reduced from 359 to 347 lines by removing blank separators only so closure would not introduce new maintainability debt. A clean-source `web full` Engineering Guard, dependency audit (0 vulnerabilities), web maintainability ratchet, and agent workspace validation all passed. Closure/integration is explicitly authorized; Plan 047 remains unstarted. Evidence: `.agents/evidence/plan-046-closure-2026-09-11.md`.

## Plan 046 integration recorded

PR #44 merged Plan 046 into `main` at `9aad66f964f01b87815893a50e86fbf95236b815` after closure governance passed. Current authority now records Plan 046 as integrated and leaves Plan 047 unstarted.

## Plan 047 implementation and physical acceptance

Plan 047 added explicit Native Explore Digital Twin 3D parity while preserving the accepted app-owned 2D positioning/navigation path as default. Phase 0 physical feasibility proved the real authenticated model path on the Android POS: workspace `003b660a-dfa5-426f-b2e5-7f8023e084a3`, building `19954`, LT1 180,332 bytes / 118 meshes and LT2 156,012 bytes / 100 meshes, rendered through authenticated Nitro GLB retrieval -> Expo GL -> mobile Three -> GLTFLoader. No WebView or `expo-three` shortcut was used.

The implementation shares only deterministic model-slot/view and semantic-room contracts with web, adds authenticated ArrayBuffer retrieval to the native client, mounts 3D lazily, keeps 2D navigation authoritative, provides room search/Go/reset/floor switching/touch look-walk, and explicitly errors rather than silently falling back when 3D is unavailable. The user accepted the product flow apart from one mode-switch placement collision; the switch was subsequently relocated into the existing map-control cluster in both modes without falsely claiming another physical retest of only that final layout change.

## Plan 047 explicit closure

The user requested closure through a clean pushed branch. Web closure validation passed `engineering-guard.sh web full` with lint, typecheck, production build, and 0 web audit findings; web maintainability also passed. Mobile closure initially exposed real maintainability debt rather than being rubber-stamped: `NativeMapScreen.tsx` exceeded its legacy file/function ceilings and the new 3D component/renderer had oversized functions. Closure refactored browse UI and native 3D runtime responsibilities without changing the product contract. The mobile ratchet now passes, with `NativeMapScreen.tsx` improved from 481 to 453 lines and its legacy oversized function from 314 to 285 lines; the baseline was lowered accordingly.

After the final refactor, mobile policy/lint/typecheck passed through `engineering-guard.sh mobile fast`, Expo public config passed, and a fresh Android Expo export bundled 776 modules into a ~3.1 MB Hermes bundle. A clean `npm ci` reapplied the existing `image-size@1.2.1` parser patch. Closure also accepted compatible non-force transitive security updates (`@expo/metro 56.0.2`, Expo Metro `0.84.5`, `js-yaml 4.3.2`). The final npm audit remains truthfully nonzero only for four aggregate high findings from the same two `image-size` advisories whose exact parser behaviors are locally remediated; scanner residuals were not hidden or dismissed.

`git diff --check`, agent workspace validation, and proof/debug residue scanning passed. No proof-only harness, credential, session token, secret, PR, or merge was persisted. The product implementation was committed as `61fd329` (`feat(mobile): add digital twin 3d parity`). Closure evidence is `.agents/evidence/plan-047-closure-2026-09-11.md`; integration remains separately user-gated.
