# Plan 047 Closure Evidence — 2026-09-11

## Scope

Plan 047 adds explicit Native Explore Digital Twin 3D parity while preserving the accepted app-owned 2D positioning/navigation path as the default. The implementation commit is `61fd329` (`feat(mobile): add digital twin 3d parity`) on branch `plan/047-native-digital-twin-parity`.

No PR or merge is part of this closure. Integration remains separately user-gated.

## Product / physical evidence carried into closure

Physical Android feasibility had already proved the production renderer path with real authenticated workspace assets, not demo geometry:

- workspace `003b660a-dfa5-426f-b2e5-7f8023e084a3`;
- building `19954`;
- LT1: 180,332 bytes / 118 meshes;
- LT2: 156,012 bytes / 100 meshes;
- Android API 30 arm64-v8a POS, Mali-G52 / OpenGL ES 3.2-class context;
- authenticated Nitro GLB endpoint -> Expo GL -> Three -> GLTFLoader -> actual floor geometry.

The user subsequently accepted the product flow with one remaining mode-switch placement issue. That control was relocated into the existing map-control clusters. Closure does not falsely claim a second user physical retest of only that final placement change.

## Maintainability remediation during closure

The first mobile maintainability run correctly rejected new debt:

- `NativeMapScreen.tsx` exceeded its legacy file/function ceilings;
- `NativeDigitalTwin.tsx` and `nativeDigitalTwinRenderer.ts` introduced oversized functions.

Closure fixed structure without changing the product contract:

- browse UI was extracted to `NativeMapBrowseOverlay.tsx`;
- native 3D motion/runtime responsibilities were split into bounded helpers;
- 3D component lifecycle/look/loading responsibilities were separated;
- `NativeMapScreen.tsx` improved from the legacy 481-line ceiling to 453 lines;
- its legacy oversized function improved from 314 to 285 lines;
- the mobile maintainability baseline was ratcheted downward accordingly, never raised.

Final mobile maintainability passed:

`maintainability validation passed: codebase=mobile scanned=25 file_debt=2 function_debt=2 directory_debt=0 bypass_debt=0`

Web maintainability had already passed for the shared/web portion:

`maintainability validation passed: codebase=web scanned=146 file_debt=0 function_debt=0 directory_debt=2 bypass_debt=0`

## Engineering Guard / build validation

### Web

`./.agents/scripts/engineering-guard.sh web full` passed after the shared/web contract extraction:

- repository policy: PASS;
- Nuxt prepare: PASS;
- lint: PASS;
- typecheck: PASS;
- production build: PASS;
- dependency audit: 0 vulnerabilities;
- final marker: `SITUM_ENGINEERING_GUARD_PASS codebase=web mode=full`.

Only the existing non-fatal large-chunk/plugin-timing build warnings were emitted.

### Mobile

After the closure refactor, `./.agents/scripts/engineering-guard.sh mobile fast` passed:

- repository policy: PASS;
- lint: PASS;
- TypeScript typecheck: PASS;
- final marker: `SITUM_ENGINEERING_GUARD_PASS codebase=mobile mode=fast`.

`npx expo config --type public` passed. A fresh Android Expo export also passed after the final refactor:

- 776 modules bundled;
- Hermes bundle about 3.1 MB;
- output `/tmp/plan047-mobile-export-final`;
- the installed `@situm/react-native` package still emits its known invalid-`exports` warning and Metro falls back to file-based resolution successfully.

## Dependency audit truth

The first mobile full guard reached and passed policy, lint, typecheck, and Expo config, then exited nonzero at `npm audit` because the published `image-size@1.2.1` version remains inside two upstream advisory ranges. The repository already carries the documented `patch-package` parser remediation for those exact ICNS/JXL/HEIF infinite-loop behaviors; npm/GitHub classify by package version and therefore continue to report the scanner residual.

Closure did not suppress or dismiss that result. A clean `npm ci` reapplied `image-size@1.2.1` patch successfully.

A compatible non-force audit remediation also removed newly actionable findings by updating only the lock-resolved compatible tree, including `@expo/metro 56.0.0 -> 56.0.2`, its Expo Metro family `0.84.4 -> 0.84.5`, and `js-yaml 4.3.1 -> 4.3.2`. No direct `metro` dependency was added to the application.

The final fresh mobile audit reports exactly four aggregate high findings, all propagating from the same two locally patched `image-size` advisories:

- `GHSA-w3rx-r6r6-pgpr`;
- `GHSA-5p2g-fcmc-qvqq`.

This is recorded as a known scanner/version-range residual under the active security-maintenance decision, not as a zero-vulnerability PASS and not as a new Plan 047 regression.

## Governance / hygiene

Final implementation-tree checks before the implementation commit:

- `git diff --check`: PASS;
- `./.agents/scripts/validate.sh`: `agent workspace validation passed`;
- proof/debug scan across changed 3D/shared surfaces: no proof-only markers, TODO/FIXME closure residue, or console debug logging;
- proof-only device harness code was not committed;
- no credential/session/secret material was persisted;
- no PR or merge was created.

## Closure conclusion

Plan 047 implementation is closure-approved on its dedicated branch. Native Explore remains 2D-first; Digital Twin 3D is explicit, authenticated, lazy, workspace/building/floor scoped, deterministic, and error-explicit with no silent fallback or fabricated 3D navigation. Integration remains pending explicit user authorization.
