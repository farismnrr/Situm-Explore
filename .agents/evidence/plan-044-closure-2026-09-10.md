# Plan 044 Closure Evidence — 2026-09-10

## Scope

Plan 044 closes the web Explore redesign on `plan/044-web-native-map-parity`: the primary browser Map experience is a 3D-only Three.js/WebGL digital-twin walkthrough over authenticated workspace-scoped GLB assets. There is no 2D floorplan/Viewer fallback. Sensor-backed positioning and turn-by-turn guidance remain native-only.

## Runtime acceptance already completed

Authenticated temporary Playwright acceptance before closure covered both configured floor models. F2 loaded a valid `200 model/gltf-binary` GLB, rendered WebGL, selected `Workroom 3`, and moved camera X from 8.450 to 3.325 while preserving camera Y at 1.620 m. F1 loaded its own valid GLB, selected `Workroom 1`, and moved camera X from 8.401 to 5.325 while preserving camera Y at 1.620 m. Temporary browser scripts/screenshots were removed and process inspection reported zero Chrome/Chromium/Playwright processes afterward. User visual E2E is not claimed as executed; the user explicitly requested governance closure before the next stage.

## Closure cleanup

Full branch review against `origin/main` removed the superseded `IndoorMapCanvas` 2D renderer, removed the duplicate specific 3D-model API route in favor of the repository's effective workspace catch-all dispatcher, split semantic-room extraction out of `IndoorWalkCanvas` so the component remains below the 350-line source ceiling, and moved the 3D model server helper into `server/integrations/situm/` so `server/utils` does not exceed its existing directory-debt ceiling. Durable architecture/design text was updated to remove the stale 2D browser Map contract.

Dependency closure refreshed `@nuxt/ui` to `^4.11.1` within the existing major line and regenerated the lockfile so vulnerable transitive resolutions are replaced. A clean `npm ci` reported `found 0 vulnerabilities`.

## Governance results

The user's unrelated PostgreSQL `public`-schema Drizzle/schema edits were stashed before clean-branch validation so product governance measured Plan 044 only.

- `git diff --check` — PASS before the final governance gates.
- focused `npm run lint` — PASS.
- focused `npm run typecheck` — PASS.
- focused `npm run build` — PASS; only non-fatal Vite chunk-size/plugin-timing warnings were emitted.
- `./.agents/scripts/validate.sh` — PASS (`agent workspace validation passed`).
- first `./.agents/scripts/engineering-guard.sh web full` attempt — STOPPED by policy because `.tmp-tests` still existed. The directory was inspected, confirmed to contain task-owned temporary GLB/probe/Playwright artifacts, and deleted before retry.
- final `./.agents/scripts/engineering-guard.sh web full` — PASS: web policy, lint, typecheck, production build, and dependency audit all passed; audit reported `found 0 vulnerabilities` and the guard emitted `SITUM_ENGINEERING_GUARD_PASS codebase=web mode=full`.
- `python3 .agents/scripts/maintainability.py web` — PASS, executed once after the successful Engineering Guard: `scanned=134 file_debt=0 function_debt=0 directory_debt=2 bypass_debt=0`. The two directory entries are existing baseline debt and did not grow.

No Engineering Guard or maintainability run is claimed for mobile because Plan 044 changed only the web codebase.

## Integration and deployment boundary

Closure commit `b39c25c6e7ae6242ab4b18b4f4e7653fae898052` (`chore(web): close 3d Explore governance`) was pushed to `origin/plan/044-web-native-map-parity`. Closure does not authorize a PR or merge; the branch remains available for user review/integration.

The user's unrelated PostgreSQL `public`-schema Drizzle/schema compatibility work was restored after the closure commit and remains unstaged/uncommitted outside Plan 044 history. Because the current staging database requires that compatibility work, the final local runtime image was intentionally rebuilt from closure commit `b39c25c` plus that restored worktree and clearly tagged `sha-b39c25c6e7ae-publicschema-local`. Only the `situm-explore` web service was force-recreated with `--no-deps`; the database service was not restarted. The container is healthy and both `/api/health/liveness` and `/` return 200. No browser automation was run during this final closure rebuild.

GHCR publication remains blocked on this host by push authorization (403), so the final staging runtime is local-only until registry authorization is fixed.
