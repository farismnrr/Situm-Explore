# Plan 046 closure evidence — 2026-09-11

## Scope

Plan 046 — Web Explore 2D Routing Foundation. Closure was explicitly requested by the user after the production-style staging runtime was recovered from a rebuild-context mistake. Plan 047 was not started.

## Runtime recovery before closure

The first E2E rebuild accidentally included unrelated uncommitted PostgreSQL `public`-schema compatibility files from the primary worktree. The resulting image queried `public.users` while the active staging database still stored application tables under `situm_explore`, causing login `500` / PostgreSQL `42P01 relation "users" does not exist`.

The issue was isolated from Plan 046 product code. Staging was rebuilt from clean committed Plan 046 source (`9b4d360c249ec747e3e126195fe88ad3f6671ada`) without the unrelated schema edits, then force-recreated on port 3005. The recreated container became healthy; liveness returned 200; an intentionally invalid login reached the database-backed auth path and returned the expected 401 rather than 500. No database migration was applied. The user's unrelated schema work remained untouched.

## Closure-only maintainability remediation

Before running the maintainability gate, `app/pages/app/map.vue` was 359 lines, exceeding the repository's 350-line source ceiling with no legacy baseline. Closure removed twelve blank separator lines only, reducing the file to 347 lines without changing runtime behavior. No maintainability baseline was expanded.

## Final closure governance

Final governance ran from a temporary clean `git archive` snapshot of Plan 046 plus only the closure-time `map.vue` whitespace reduction. This explicitly excluded unrelated dirty Drizzle/PostgreSQL files and unrelated Situm Path investigation artifacts.

Results:

- `./.agents/scripts/engineering-guard.sh web full` — PASS
  - web codebase policy — PASS
  - Nuxt prepare — PASS
  - `npm run lint` — PASS
  - `npm run typecheck` — PASS
  - production `npm run build` — PASS
  - `npm audit --audit-level=high` — PASS, 0 vulnerabilities
- `python3 .agents/scripts/maintainability.py web` — PASS
  - scanned 144 source files
  - file debt 0
  - function debt 0
  - directory debt remains the existing baseline count of 2
  - bypass debt 0
- `./.agents/scripts/validate.sh` — PASS in the clean snapshot. A non-blocking `fatal: not a git repository` diagnostic was emitted by a Git lookup because `git archive` intentionally contains no `.git` directory; the validator itself completed successfully.

Existing non-fatal Nuxt/Vite chunk-size and plugin-timing warnings remain unchanged.

## Integration authorization

The user explicitly requested closure on 2026-09-11. This authorizes the Plan 046 PR/merge/branch-close sequence under `.agents/protocols/git-workflow.md`. Plan 047 remains outside this closure and must not start implicitly.

## Isolation

Closure must not stage, commit, reset, clean, stash, overwrite, or otherwise absorb the unrelated local PostgreSQL/Drizzle work or the unrelated Situm Path investigation files/session hunks.
